#include<stdio.h>
int main()
{
	int n,num,i,h=0;
	printf("Enter the number of numbers:");
	scanf("%d",&n);
	i=1;
	while(i<=n)
	{
		scanf("%d",&num);
		if(i==1)
		h=num;
		if(num<h)
		h=num; 
		i++;
	}
		printf("Least among %d numbers:",n);
		printf("%d",h);
		return 0;
}
